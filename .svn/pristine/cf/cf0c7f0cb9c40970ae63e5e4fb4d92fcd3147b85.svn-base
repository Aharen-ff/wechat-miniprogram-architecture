import { request } from '../utils/request';

export function login(params) {
  return request.get({
    url: '/aquaticProduct/login.do?action=mobileLogin&dataType=mobile',
    noToken: true,
    params,
  });
}
